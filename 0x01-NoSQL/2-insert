#!/usr/bin/env mongo
// This script inserts a document into the collection "school".

// Get the database name from the command line arguments
var dbName = db.getSiblingDB("{{DB_NAME}}");

// Insert the document into the "school" collection
dbName.school.insertOne({ name: "Holberton school" });
